<%= simple_form_for(@text) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :title %>
    <%= f.input :content %>
    <%= f.association :books %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
