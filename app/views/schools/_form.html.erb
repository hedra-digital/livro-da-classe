<%= simple_form_for(@school) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name %>
    <%= f.association :books %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
