<!DOCTYPE html>
<html class="no-js">
    <head>
        <meta charset="utf-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1">
        <title><%= @book.title %></title>
        <meta name="description" content="">
        <meta name="viewport" content="width=device-width">
        <meta name="apple-mobile-web-app-capable" content="yes">


        <!-- EPUBJS Renderer -->
        <%= javascript_include_tag "application" %>

        <style type="text/css">

          body {
            overflow: hidden;
          }

          #area {
            width: 400px;
            height: 600px;
            margin: 0 auto;
          }

          #area iframe {
            border: none;
          }

          #prev {
            left: 40px;
          }

          #next {
            right: 40px;
          }

          .arrow {
            position: absolute;
            top: 50%;
            margin-top: -32px;
            font-size: 64px;
            color: #E2E2E2;
            font-family: arial, sans-serif;
            font-weight: bold;
            cursor: pointer;
            -webkit-user-select: none;
            -moz-user-select: none;
            user-select: none;
          }

          .download a {
            position: absolute;
            margin-top: 30px;
            margin-left: 21px;
            font-size: 30px;
            color: #C1C1C1;
            font-family: arial, sans-serif;
            font-weight: bold;
            cursor: pointer;
            -webkit-user-select: none;
            -moz-user-select: none;
            user-select: none;
            text-decoration: none;
          }

          .back {
            position: absolute;
            font-size: 30px;
            color: #C1C1C1;
            font-family: arial, sans-serif;
            font-weight: bold;
            cursor: pointer;
            -webkit-user-select: none;
            -moz-user-select: none;
            user-select: none;
          }

          .download:hover,
          .arrow:hover {
            color: #777;
          }

          .arrow:active {
            color: #000;
          }
        </style>

         <script>
            "use strict";
            var Book = ePub('../../<%= @book.directory.to_s.gsub("public/","") + "/EBOOK.epub" %>', {
              width: 400,
              height: 600
            });
        </script>
    </head>
    <body>
        <div id="main">
          <div id="back" onclick="back_to_book();" class="back">‹ Voltar ao Livro</div>
          <div id="download" class="download">
            <a href='../../<%= @book.directory.to_s.gsub("public/","") + "/EBOOK.epub" %>'>
              Download<a>
          </div>
          <div id="prev" onclick="Book.prevPage();" class="arrow">‹</div>
          <div id="wrapper">
            <div id="area"></div>
          </div>
          <div id="next" onclick="Book.nextPage();" class="arrow">›</div>
        </div>

        <script>
            Book.renderTo("area")

            function back_to_book() {
              current_url = window.location.href.replace("\epub_viewer", "")
              window.location =  current_url;
            }
        </script>
    </body>
</html>
