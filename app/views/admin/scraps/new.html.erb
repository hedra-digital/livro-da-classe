<h3><%= "Nova Conversa" %></h3>

<%= simple_form_for([:admin, @scrap], :html => {:class => 'form-vertical' }) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :book_id, :collection => @books, :include_blank => false %>
    <%= f.input :content, :as => :text, :label => 'Conteúdo' %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>