<h1>Novo colaborador</h1>

<%= simple_form_for([@book, @collaborator], :url => book_collaborators_path) do |f| %>
  <div class="form-inputs">
    <%= f.input :email %>
  </div>

  <div class="form-actions">
    <%= f.button :submit, "Enviar convite" %>
  </div>
<% end %>
