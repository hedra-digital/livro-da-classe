<section id="new_session">
  <div class="new_session cadastro-login">
  <!--
    <ul class="social-connect">
      <span>Conecte-se com</span>
      <li class="facebook"><%= link_to '', '/auth/facebook', :id => 'facebook-oauth' %></li>
      <li class="twitter"><%= link_to '', '/auth/twitter', :id => 'twitter-oauth' %></li>
      <li class="google"><%= link_to '', '/auth/google_oauth2', :id => 'google-oauth' %></li>
    </ul>
  -->
    <div class="tabs">
      <%= link_to "Login", signin_path, class: "active" %>
      <%= link_to "Cadastre-se", new_user_path %>
    </div>

    <h1>Entrar no site</h1>
    <%= render partial: 'form' %>
  </div>
</section>

<script>
  $.each($("form div"), function() {
    var label = $("label",$(this)).text();
    $("label",$(this)).hide();
    $("div input",$(this)).prop("placeholder",label);
  });
</script>
